<div class="container">
	<div class="row">
		<div class="col-md-4"></div>
		<div class="col-md-4">
			<h1>Eat-Da-Burger</h1>
		</div>
		<div class="col-md-4"></div>
	</div>

	<div class="row">
		<div class="col-md-6">
			
			{{#each burgers}}
				{{#unless devoured}}
			<div class="alert alert-light" role="alert"></div>
			  {{id}} . {{burger_name}}
			  <button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
			  	Devour it!
			  </button>
			</div>
				{{/unless}}
			{{/each}}
		</div>

		<div class="col-md-6">
			
			{{#each burgers}}
				{{#if devoured}}
					<div class="alert alert-dark" role="alert"></div>
					  {{id}} . {{burger_name}}
					</div>
				{{/if}}
			{{/each}}
		</div>
	</div>

  <div class="col s12 m7">
    <h2 class="header">Add a Burger</h2>
    <div class="card horizontal">
      <div class="card-stacked">
        <div class="card-content">
          <p>Enter a burger you want to eat and click the "Submit" button. You can be as creative as possible - you have a galaxy!</p>
        </div>
      </div>
    </div>
  </div>

    <div class="row">
    <form class="col s12">
      <div class="row">
        <div class="input-field col s12">
          <textarea id="textarea1" class="materialize-textarea"></textarea>
          <label for="textarea1">Textarea</label>
           <button class="btn waves-effect waves-light" type="submit" name="action">Submit</button>
        </div>
      </div>
    </form>
  </div>
        